DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE_NAME"
AUTH_SECRET="replace-with-a-long-random-secret"
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
CRON_SECRET="replace-with-a-secure-random-token"
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
# Must match your Blob store: "public" (default) or "private". Private stores require BLOB_STORE_ACCESS=private and staff open files via the app (not direct CDN URLs).
BLOB_STORE_ACCESS="public"
